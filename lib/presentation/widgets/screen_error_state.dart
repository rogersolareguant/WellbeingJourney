import 'package:flutter/material.dart';

class ScreenErrorState extends StatelessWidget {
  const ScreenErrorState({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Error'),
    );
  }
}
